fastapi>=0.111
uvicorn[standard]>=0.30
pydantic[email]>=2.7
pydantic-settings>=2.3
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
pyjwt>=2.8
passlib[bcrypt]>=1.7
bcrypt==4.0.1
# ^ PRODUCTION FIX (integration audit): passlib 1.7.4 (last release, unmaintained
# since ~2020) is incompatible with bcrypt>=4.1's stricter 72-byte handling —
# every register/login call raised "ValueError: password cannot be longer than
# 72 bytes" from inside passlib's own internal self-test, unrelated to actual
# password length. Pinned to the last bcrypt release passlib works with. This
# is a well-known, widely-documented passlib/bcrypt interaction, not specific
# to this codebase.
livekit-api>=1.2,<2.0
# ^ PRODUCTION FIX (integration audit): was unpinned >=0.7, which resolved to
# 1.2.0 — a major version with a renamed AccessToken builder method (see
# app/features/media/service.py). Pinned to the major version this code was
# updated against.
redis>=5.0
python-multipart>=0.0.9
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
